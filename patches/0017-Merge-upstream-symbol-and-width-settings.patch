From 708092dec472047beb54df11b27173be5a9a68b8 Mon Sep 17 00:00:00 2001
From: Yuya Fujiwara <hzw1258@gmail.com>
Date: Fri, 14 Aug 2026 21:14:59 +0900
Subject: [PATCH 17/18] Merge upstream symbol and width settings

---
 karukan-im/core/src/config/settings.rs        |  8 +++----
 karukan-im/core/src/core/engine/mod.rs        |  3 ++-
 .../core/src/core/engine/tests/basic.rs       | 13 -----------
 .../src/core/engine/tests/live_conversion.rs  | 22 -------------------
 4 files changed, 6 insertions(+), 40 deletions(-)

diff --git a/karukan-im/core/src/config/settings.rs b/karukan-im/core/src/config/settings.rs
index 4efa783..9fd106f 100644
--- a/karukan-im/core/src/config/settings.rs
+++ b/karukan-im/core/src/config/settings.rs
@@ -28,6 +28,10 @@ pub struct Settings {
     pub symbol: SymbolSettings,
     /// The width kana input comes out at, per character group
     pub width: WidthRules,
+    /// Key-binding behavior settings
+    pub keys: KeysSettings,
+    /// Date-conversion settings (きょう -> today's date)
+    pub date: DateSettings,
 }
 
 /// The space the Space key inputs while typing kana. Alphabet and emoji
@@ -67,10 +71,6 @@ impl SymbolSettings {
             slash: self.slash,
         }
     }
-    /// Key-binding behavior settings
-    pub keys: KeysSettings,
-    /// Date-conversion settings (きょう -> today's date)
-    pub date: DateSettings,
 }
 
 /// Aux-line settings
diff --git a/karukan-im/core/src/core/engine/mod.rs b/karukan-im/core/src/core/engine/mod.rs
index 9544e98..ef6d9bb 100644
--- a/karukan-im/core/src/core/engine/mod.rs
+++ b/karukan-im/core/src/core/engine/mod.rs
@@ -29,7 +29,7 @@ mod tests;
 
 use karukan_engine::{
     DateRewriter, Dictionary, EmojiRewriter, KanaKanjiConverter, LearningCache, LearningConfig,
-    RewriteOutput, Rewriter, RewriterChain, RomajiConverter,
+    RewriteOutput, RewriterChain, RomajiConverter,
 };
 use tracing::{debug, trace};
 
@@ -232,6 +232,7 @@ impl InputMethodEngine {
         if config.date_conversion && !config.date_formats.is_empty() {
             chain.add(Box::new(DateRewriter::new(config.date_formats.clone())));
         }
+        chain
     }
 
     /// Conversion (inference) time of the last `process_key` /
diff --git a/karukan-im/core/src/core/engine/tests/basic.rs b/karukan-im/core/src/core/engine/tests/basic.rs
index 4f9392d..b487f64 100644
--- a/karukan-im/core/src/core/engine/tests/basic.rs
+++ b/karukan-im/core/src/core/engine/tests/basic.rs
@@ -58,19 +58,6 @@ fn test_engine_backspace() {
     assert!(matches!(engine.state(), InputState::Empty));
 }
 
-#[test]
-fn space_in_empty_passes_through_by_default() {
-    // The default space is the ASCII one, which the application inserts
-    // itself — leaving Space to do whatever it does there (scrolling a
-    // page) while the IME has nothing to compose.
-    let mut engine = InputMethodEngine::new();
-
-    let result = engine.process_key(&press_key(Keysym::SPACE));
-    assert!(!result.consumed);
-    assert!(matches!(engine.state(), InputState::Empty));
-    assert!(result.actions.is_empty());
-}
-
 #[test]
 fn space_in_empty_hiragana_commits_fullwidth_space() {
     // A full-width space is committed directly from Empty without entering
diff --git a/karukan-im/core/src/core/engine/tests/live_conversion.rs b/karukan-im/core/src/core/engine/tests/live_conversion.rs
index f23d41f..0b192c9 100644
--- a/karukan-im/core/src/core/engine/tests/live_conversion.rs
+++ b/karukan-im/core/src/core/engine/tests/live_conversion.rs
@@ -336,28 +336,6 @@ fn test_shift_space_alone_commits_a_fullwidth_space() {
     }
 }
 
-#[test]
-fn test_shift_space_inserts_the_configured_space_into_a_composition() {
-    // Bare Space converts here, so this chord is the only way to put a
-    // space into a composition — and it takes the everyday width, so a
-    // half-width space can be part of what gets converted.
-    for (mut engine, expected) in [
-        (InputMethodEngine::new(), "あ "),
-        (fullwidth_space_engine(), "あ\u{3000}"),
-    ] {
-        engine.process_key(&press('a'));
-        engine.process_key(&press_shift_key(Keysym::SPACE));
-        assert_eq!(engine.preedit().unwrap().text(), expected);
-
-        let result = engine.process_key(&press_key(Keysym::RETURN));
-        let commit = result.actions.iter().find_map(|a| match a {
-            EngineAction::Commit(text) => Some(text.clone()),
-            _ => None,
-        });
-        assert_eq!(commit.as_deref(), Some(expected));
-    }
-}
-
 // --- Ctrl+Shift+L live conversion toggle tests ---
 
 #[test]
-- 
2.55.0

